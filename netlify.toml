# Static publish — no build step. The page is one self-contained index.html and
# giveaways.json is regenerated by .github/workflows/scrape.yml, so a deploy is
# just a file copy.

[build]
  publish = "public"

[build.processing]
  skip_processing = true
